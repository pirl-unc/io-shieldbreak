# Extraction template

> **This file is written by the `trialist_screener` agent**, not by hand.
>
> On first invocation the agent will propose a default column schema based on the search topic, walk you through adding/removing/renaming columns, and then write the result here. The machine-readable mirror lives at [`data/schema.json`](../data/schema.json).
>
> Hand-edits between runs are allowed — the agent will surface the diff and confirm before treating them as authoritative. Schema-altering edits trigger a version bump in `data/schema.json`.

_(empty — populated on first run)_
